import { Product, Category } from '@/types/product';

// Dados agora vêm do banco de dados Supabase
// Este arquivo mantém apenas as estruturas de tipos para referência

export const categories: Category[] = [];

export const products: Product[] = [];